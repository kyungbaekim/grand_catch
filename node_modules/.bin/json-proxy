#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../json-proxy/bin/json-proxy" "$@"
  ret=$?
else 
  node  "$basedir/../json-proxy/bin/json-proxy" "$@"
  ret=$?
fi
exit $ret
